import { useEffect } from 'react';

const projects = [
  { title: 'Sneaker Vault', description: 'GSAP animations and responsive design' },
  { title: 'Tic-Tac-Toe Game', description: 'React game with undo feature' },
];

const Projects = () => {

  return (
    <div className='h-screen w-full bg-purple-900/20'>

    </div>
  );
};

export default Projects;
